# GCP - Serverless and On-demand scaling

- e.g. Have build a python application using flask. -> Have chosen 'App Engine' to deploy -> 

Initial config:
Min instance = 1
Max instance = 2

When load increases, app engine automatically scales.

